| 阶段    | 中间件           |                       最终推荐版本 | Docker / K8s 部署建议                                                           | 备注                                            |
|-------|---------------|-----------------------------:|-----------------------------------------------------------------------------|-----------------------------------------------|
| 第一批核心 | MySQL         |                    8.4.8 LTS | 固定到精确版本 tag，不使用 `latest`                                                    | 控制面元数据、发布表、管理数据、一期查询日志主库；已与 `deploy` 默认镜像保持一致 |
| 第一批核心 | Redis         |                        7.4.7 | 固定到精确版本 tag，不使用 `latest`                                                    | 名单、画像、热点特征副本、低延迟 lookup                       |
| 第一批核心 | Kafka         |                 3.9.1（KRaft） | **直接使用 KRaft 模式**，不再单独部署 ZooKeeper；Docker/K8s 建议使用官方镜像 `apache/kafka:3.9.1` | 事件总线、决策结果流、日志流、异常流                            |
| 第二批分析 | Doris         |                        3.1.4 | 作为独立分析层部署，建议与在线链路解耦                                                         | 决策日志、报表分析、聚合查询                                |
| 按需补充  | Elasticsearch |                       8.18.8 | 仅在需要全文检索/日志检索时部署                                                            | 不替代 Doris，主要承担检索能力                            |
| 监控必选  | Prometheus    |                    3.5.1 LTS | **固定版本，不跟随 chart 默认 appVersion 漂移**                                         | 生产基线版本，建议与 Grafana 配套部署                       |
| 监控必选  | Grafana       |                       12.4.0 | 使用 `grafana/grafana` 或 `grafana/grafana-enterprise`，固定精确版本                  | 生产基线版本，适合作为默认监控 UI                            |
| 运维增强  | Kafka UI      |                        1.4.2 | 单独部署即可，建议仅内网暴露                                                              | 用于 topic、consumer lag、消息排障                    |
| 对象存储  | MinIO         | RELEASE.2025-10-15T17-29-55Z | 固定到精确 release tag                                                           | 文件存储、对象存储；后续可承接回放文件、归档等场景                     |
