# 项目一期定位

- 不做在线同步决策api, 仅提供异步风控链路, 风控动作仅支持 PASS, TAG_ONLY, REVIEW, REJECT 四种, 业务系统或下游系统需要具备异步处理风控结果的能力.
- redis只做名单,不做画像和热点特征. 页面提供名单管理, 支持手动同步数据到redis.
- 只做 FIRST_HIT, 忽略其他

# 架构图

```mermaid
flowchart LR
    subgraph IN["接入层"]
        FE["业务系统前端"]
        BE["业务系统后端"]
        NGINX["Nginx"]
        SDK["pulsix-sdk<br/>Netty Client"]
        INGEST["pulsix-ingest<br/>鉴权 / 数据清洗 / 标准化"]

        FE --> NGINX --> INGEST
        BE --> SDK --> INGEST

        INGEST -->|正确数据| EVT["Kafka<br/>pulsix.event.standard"]
        INGEST -->|非法数据| DLQ["Kafka<br/>pulsix.event.dlq"]
        INGEST -->|接入异常 / 解析错误| IERR["Kafka<br/>pulsix.ingest.error"]
    end

    subgraph CTRL["控制面 / 管理面"]
        UI["pulsix-ui<br/>风控控制台"]
        SERVER["pulsix-server<br/>+ pulsix-module-risk"]
        MYSQL["MySQL<br/>场景 / 名单 / 特征 / 规则 / 策略 / 发布快照"]
        CDC["MySQL CDC<br/>scene_release"]
        REDIS["Redis<br/>名单"]
        ALERTCFG["预警配置<br/>模板 / 渠道 / 级别 / 静默"]

        UI --> SERVER
        SERVER --> MYSQL
        MYSQL --> CDC
        SERVER -->|名单导入 / 同步 Redis| REDIS
        SERVER --> ALERTCFG
    end

    subgraph COMP["计算面"]
        ENGINE["pulsix-engine<br/>Flink Decision Engine"]
        STATE["Flink State<br/>Broadcast / Keyed / Timer"]

        EVT --> ENGINE
        CDC --> ENGINE
        REDIS -->|名单 lookup| ENGINE
        STATE --- ENGINE

        ENGINE --> KRES["Kafka<br/>pulsix.decision.result"]
        ENGINE --> KLOG["Kafka<br/>pulsix.decision.log"]
        ENGINE -->|计算异常| EERR["Kafka<br/>pulsix.engine.error"]
    end

    subgraph ANALYZE["分析 / 查询 / 下游消费"]
        DORIS["Doris<br/>结果 / 日志 / 错误查询模型<br/>traceId / eventId 可追踪"]
        BSUB["下游系统 / 业务系统后端"]

        KRES --> DORIS
        KLOG --> DORIS
        DLQ --> DORIS
        IERR --> DORIS
        EERR --> DORIS

        KRES --> BSUB
        DORIS --> SERVER
    end

    subgraph ALERT["实时预警"]
        ALERTC["风险预警消费者<br/>TAG_ONLY / DLQ / 错误流"]
        GOVERN["告警治理<br/>去重 / 限流 / 静默 / 升级 / 重试"]
        GATEWAY["通知网关 / 渠道适配层"]
        ALERTDB["告警记录 / 风险事件<br/>traceId / eventId 关联"]
        FEISHU["飞书 Webhook"]
        DING["钉钉 Webhook"]
        WECOM["企业微信 Webhook"]
        HOOK["通用 Webhook"]

        KRES -->|TAG_ONLY + 告警配置| ALERTC
        DLQ --> ALERTC
        IERR --> ALERTC
        EERR --> ALERTC
        ALERTCFG --> ALERTC

        ALERTC --> GOVERN
        GOVERN --> ALERTDB
        GOVERN --> GATEWAY

        GATEWAY --> FEISHU
        GATEWAY --> DING
        GATEWAY --> WECOM
        GATEWAY --> HOOK

        ALERTDB --> SERVER
    end

    subgraph OBS["观测中心"]
        PROM["Prometheus"]
        GRAF["Grafana"]
        ALERTM["Alertmanager"]

        INGEST -. 指标: 错误率 / 延迟 .-> PROM
        ENGINE -. 指标: 背压 / Checkpoint / 延迟 / 异常 .-> PROM
        SERVER -. 指标: 管理面 / 消费者状态 .-> PROM

        PROM --> GRAF
        PROM --> ALERTM
    end

    ALERTM --> GATEWAY
```

# 项目模块

```
pulsix/
├── pulsix-dependencies/              # BOM / 版本对齐
├── pulsix-access/
│   ├── pulsix-ingest/               # 接入层服务端统一接入器 (Netty Server)
│   └── pulsix-sdk/                  # 业务后端高性能接入 SDK (Netty Client)
├── pulsix-framework/
│   ├── pulsix-common/               # 公共工具、常量、共享 DTO/CommonApi
│   ├── pulsix-kernel/               # 执行内核（仿真 + Flink 共用）
│   └── pulsix-spring-boot-starter-* # 各类基础组件
├── pulsix-server/                   # Spring Boot 启动器
├── pulsix-module-system/            # 用户、权限、租户、菜单、审计
├── pulsix-module-infra/             # 配置、文件、任务、监控、基础日志
├── pulsix-module-risk/              # 风控控制面核心业务+实时预警(飞书,钉钉,webhook等)
├── pulsix-engine/                   # Flink 决策引擎
├── pulsix-ui/                       # Vue3 控制台
├── deploy/                          # docker-compose / shell 脚本
├── docs/                            # 架构图、时序图、设计文档
└── README.md
```

